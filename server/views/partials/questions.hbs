<span>Only one per question:</span>
<div style='display: flex; flex-direction:column'>
  {{#each questions}}
    <label for='html'>{{question}}
      {{#each answers}}
        <input type='radio' value='html' id='html' name='choice{{../id}}' />
        {{content}}
      {{/each}}
    </label>
  {{/each}}

</div>